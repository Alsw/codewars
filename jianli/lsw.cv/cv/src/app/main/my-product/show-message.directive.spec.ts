import { ShowMessageDirective } from './show-message.directive';

describe('ShowMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMessageDirective();
    expect(directive).toBeTruthy();
  });
});
