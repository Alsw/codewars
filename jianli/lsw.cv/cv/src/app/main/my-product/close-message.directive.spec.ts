import { CloseMessageDirective } from './close-message.directive';

describe('CloseMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseMessageDirective();
    expect(directive).toBeTruthy();
  });
});
